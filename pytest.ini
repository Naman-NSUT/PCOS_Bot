[pytest]
# Only collect from tests/. Without this, any root-level test_*.py scratch file
# is imported during collection — and if it calls the LLM at module scope, a
# bare `pytest` silently makes a real, billed API request.
testpaths = tests
